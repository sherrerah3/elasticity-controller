.venv
*.venv
*__pycache__

# datos crudos de la caracterizacion k6 (pesados, no se versionan)...
results/*
# ...pero SI se versiona la evidencia clave: los logs de decisiones y su README
!results/decisions.jsonl
!results/provisioning.jsonl
!results/README.md

# documentacion de estudio del codigo (no se versiona)
docs/

# binario compilado del controller (se genera con go build)
autoscaler/autoscaler-controller

# Claves privadas (labsuser.pem de AWS Academy, etc.)
*.pem

# Terraform: providers descargados, estado (puede contener secretos) y tfvars
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
crash.log